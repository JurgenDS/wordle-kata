<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0
         http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-starter-parent</artifactId>
        <version>3.4.1</version>
        <relativePath/>
    </parent>

    <groupId>com.example</groupId>
    <artifactId>hello-world-backend</artifactId>
    <version>1.0.0-SNAPSHOT</version>
    <name>Hello World Backend</name>
    <description>Hello World REST API following Hexagonal Architecture</description>

    <properties>
        <java.version>21</java.version>
        <maven.compiler.source>21</maven.compiler.source>
        <maven.compiler.target>21</maven.compiler.target>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
        <vavr.version>0.10.4</vavr.version>
        <lombok.version>1.18.42</lombok.version>
        <mapstruct.version>1.6.3</mapstruct.version>
        <assertj.version>3.24.2</assertj.version>
        <spotbugs.version>4.8.6</spotbugs.version>
        <spotbugs-maven-plugin.version>4.8.6.6</spotbugs-maven-plugin.version>
        <findsecbugs.version>1.13.0</findsecbugs.version>
        <checkstyle.version>10.21.4</checkstyle.version>
        <checkstyle-maven-plugin.version>3.6.0</checkstyle-maven-plugin.version>
        <license-maven-plugin.version>2.5.0</license-maven-plugin.version>
        <pitest.version>1.18.2</pitest.version>
        <pitest-junit5.version>1.2.2</pitest-junit5.version>
        <!-- Default empty value for JaCoCo args (JaCoCo will override when running tests) -->
        <surefire.jacoco.args></surefire.jacoco.args>
    </properties>

    <dependencies>
        <!-- Spring Boot -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-web</artifactId>
        </dependency>

        <!-- Vavr for Either type -->
        <dependency>
            <groupId>io.vavr</groupId>
            <artifactId>vavr</artifactId>
            <version>${vavr.version}</version>
        </dependency>

        <!-- Lombok -->
        <dependency>
            <groupId>org.projectlombok</groupId>
            <artifactId>lombok</artifactId>
            <version>${lombok.version}</version>
            <scope>provided</scope>
        </dependency>

        <!-- MapStruct -->
        <dependency>
            <groupId>org.mapstruct</groupId>
            <artifactId>mapstruct</artifactId>
            <version>${mapstruct.version}</version>
        </dependency>

        <!-- Testing -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-test</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.assertj</groupId>
            <artifactId>assertj-core</artifactId>
            <version>${assertj.version}</version>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>com.tngtech.archunit</groupId>
            <artifactId>archunit-junit5</artifactId>
            <version>1.3.0</version>
            <scope>test</scope>
        </dependency>
    </dependencies>

    <build>
        <plugins>
            <plugin>
                <groupId>org.springframework.boot</groupId>
                <artifactId>spring-boot-maven-plugin</artifactId>
                <configuration>
                    <excludes>
                        <exclude>
                            <groupId>org.projectlombok</groupId>
                            <artifactId>lombok</artifactId>
                        </exclude>
                    </excludes>
                </configuration>
            </plugin>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-compiler-plugin</artifactId>
                <version>3.13.0</version>
                <configuration>
                    <release>21</release>
                    <annotationProcessorPaths>
                        <path>
                            <groupId>org.projectlombok</groupId>
                            <artifactId>lombok</artifactId>
                            <version>${lombok.version}</version>
                        </path>
                        <path>
                            <groupId>org.mapstruct</groupId>
                            <artifactId>mapstruct-processor</artifactId>
                            <version>${mapstruct.version}</version>
                        </path>
                    </annotationProcessorPaths>
                </configuration>
            </plugin>
            <!-- Maven Surefire Plugin for running tests with suppressed warnings -->
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-surefire-plugin</artifactId>
                <version>3.2.5</version>
                <configuration>
                    <!-- Merge JaCoCo agent with JVM arguments for warning suppression -->
                    <!-- Use @{...} for late binding - resolved at runtime after JaCoCo sets the property -->
                    <argLine>
                        @{surefire.jacoco.args}
                        --add-opens java.base/java.lang=ALL-UNNAMED
                        --add-opens java.base/java.util=ALL-UNNAMED
                        --add-opens java.base/java.lang.reflect=ALL-UNNAMED
                        --add-opens java.base/java.text=ALL-UNNAMED
                        --add-opens java.desktop/java.awt.font=ALL-UNNAMED
                        --enable-native-access=ALL-UNNAMED
                        -Djdk.attach.allowAttachSelf=true
                        -XX:+EnableDynamicAgentLoading
                        -Djdk.instrument.traceUsage=false
                        -XX:+UnlockExperimentalVMOptions
                        -XX:+UseEpsilonGC
                    </argLine>
                    <systemPropertyVariables>
                        <!-- Suppress ArchUnit ClassFileProcessor warnings -->
                        <archunit.analyzeJdkClasses>false</archunit.analyzeJdkClasses>
                        <!-- Suppress Spring Boot logging warnings during tests -->
                        <logging.level.com.tngtech.archunit.core.importer.ClassFileProcessor>ERROR</logging.level.com.tngtech.archunit.core.importer.ClassFileProcessor>
                    </systemPropertyVariables>
                </configuration>
            </plugin>
            <plugin>
                <groupId>org.jacoco</groupId>
                <artifactId>jacoco-maven-plugin</artifactId>
                <version>0.8.12</version>
                <configuration>
                    <excludes>
                        <!-- Exclude Spring Boot main class - framework boilerplate -->
                        <exclude>**/HelloWorldApplication.class</exclude>
                        <!-- Exclude all JDK classes to prevent instrumentation errors -->
                        <exclude>sun/**</exclude>
                        <exclude>jdk/**</exclude>
                        <exclude>com/sun/**</exclude>
                        <exclude>java/**</exclude>
                        <exclude>javax/**</exclude>
                        <exclude>org/omg/**</exclude>
                    </excludes>
                    <!-- Merge with Surefire's argLine instead of replacing it -->
                    <propertyName>surefire.jacoco.args</propertyName>
                    <!-- Append exclusions to agent arguments -->
                    <append>true</append>
                </configuration>
                <executions>
                    <execution>
                        <goals>
                            <goal>prepare-agent</goal>
                        </goals>
                        <configuration>
                            <!-- Only include our application code, effectively excluding all JDK classes -->
                            <!-- This prevents JaCoCo from trying to instrument JDK classes that may -->
                            <!-- have class file versions it doesn't support (e.g., version 69) -->
                            <includes>
                                <include>com/example/hello/**</include>
                            </includes>
                        </configuration>
                    </execution>
                    <execution>
                        <id>report</id>
                        <phase>prepare-package</phase>
                        <goals>
                            <goal>report</goal>
                        </goals>
                    </execution>
                    <execution>
                        <id>jacoco-check</id>
                        <goals>
                            <goal>check</goal>
                        </goals>
                        <configuration>
                            <rules>
                                <rule>
                                    <element>PACKAGE</element>
                                    <limits>
                                        <limit>
                                            <counter>LINE</counter>
                                            <value>COVEREDRATIO</value>
                                            <minimum>1.00</minimum>
                                        </limit>
                                        <limit>
                                            <counter>BRANCH</counter>
                                            <value>COVEREDRATIO</value>
                                            <minimum>1.00</minimum>
                                        </limit>
                                    </limits>
                                </rule>
                            </rules>
                        </configuration>
                    </execution>
                </executions>
            </plugin>
            <plugin>
                <groupId>org.codehaus.mojo</groupId>
                <artifactId>exec-maven-plugin</artifactId>
                <version>3.1.0</version>
                <executions>
                    <execution>
                        <id>print-coverage</id>
                        <phase>prepare-package</phase>
                        <goals>
                            <goal>exec</goal>
                        </goals>
                        <configuration>
                            <executable>sh</executable>
                            <arguments>
                                <argument>scripts/print-coverage.sh</argument>
                            </arguments>
                        </configuration>
                    </execution>
                </executions>
            </plugin>
            <!-- OWASP Dependency Check for security scanning -->
            <plugin>
                <groupId>org.owasp</groupId>
                <artifactId>dependency-check-maven</artifactId>
                <version>12.1.9</version>
                <configuration>
                    <failBuildOnCVSS>7</failBuildOnCVSS>
                    <formats>
                        <format>HTML</format>
                        <format>JSON</format>
                    </formats>
                    <suppressionFiles>
                        <suppressionFile>owasp-suppressions.xml</suppressionFile>
                    </suppressionFiles>
                </configuration>
                <executions>
                    <execution>
                        <goals>
                            <goal>check</goal>
                        </goals>
                    </execution>
                </executions>
            </plugin>
            <!-- SpotBugs with FindSecBugs for static analysis and security bugs -->
            <!-- NOTE: Requires SpotBugs 4.9.x with BCEL 6.11 for Java 25 support -->
            <!-- See: https://github.com/spotbugs/spotbugs/issues/3564 -->
            <plugin>
                <groupId>com.github.spotbugs</groupId>
                <artifactId>spotbugs-maven-plugin</artifactId>
                <version>${spotbugs-maven-plugin.version}</version>
                <configuration>
                    <effort>Max</effort>
                    <threshold>Medium</threshold>
                    <failOnError>true</failOnError>
                    <!-- Only analyze application code, skip JDK classes -->
                    <onlyAnalyze>com.example.hello.-</onlyAnalyze>
                    <includeFilterFile>spotbugs-include.xml</includeFilterFile>
                    <excludeFilterFile>spotbugs-exclude.xml</excludeFilterFile>
                    <plugins>
                        <plugin>
                            <groupId>com.h3xstream.findsecbugs</groupId>
                            <artifactId>findsecbugs-plugin</artifactId>
                            <version>${findsecbugs.version}</version>
                        </plugin>
                    </plugins>
                </configuration>
                <dependencies>
                    <dependency>
                        <groupId>com.github.spotbugs</groupId>
                        <artifactId>spotbugs</artifactId>
                        <version>${spotbugs.version}</version>
                    </dependency>
                </dependencies>
            </plugin>
            <!-- Checkstyle for code style enforcement -->
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-checkstyle-plugin</artifactId>
                <version>${checkstyle-maven-plugin.version}</version>
                <dependencies>
                    <dependency>
                        <groupId>com.puppycrawl.tools</groupId>
                        <artifactId>checkstyle</artifactId>
                        <version>${checkstyle.version}</version>
                    </dependency>
                </dependencies>
                <configuration>
                    <configLocation>checkstyle.xml</configLocation>
                    <consoleOutput>true</consoleOutput>
                    <failsOnError>true</failsOnError>
                    <linkXRef>false</linkXRef>
                    <sourceDirectories>
                        <sourceDirectory>${project.build.sourceDirectory}</sourceDirectory>
                    </sourceDirectories>
                </configuration>
            </plugin>
            <!-- License Maven Plugin for dependency license compliance -->
            <plugin>
                <groupId>org.codehaus.mojo</groupId>
                <artifactId>license-maven-plugin</artifactId>
                <version>${license-maven-plugin.version}</version>
                <configuration>
                    <!-- Allowed licenses (permissive open source + common Java licenses) -->
                    <!-- License names vary by library, so we include many variations -->
                    <includedLicenses>
                        <!-- Apache variants -->
                        <includedLicense>Apache-2.0</includedLicense>
                        <includedLicense>Apache 2.0</includedLicense>
                        <includedLicense>Apache License 2.0</includedLicense>
                        <includedLicense>Apache License, Version 2.0</includedLicense>
                        <includedLicense>The Apache License, Version 2.0</includedLicense>
                        <includedLicense>The Apache Software License, Version 2.0</includedLicense>
                        <!-- MIT variants -->
                        <includedLicense>MIT</includedLicense>
                        <includedLicense>MIT License</includedLicense>
                        <includedLicense>The MIT License</includedLicense>
                        <!-- BSD variants -->
                        <includedLicense>BSD</includedLicense>
                        <includedLicense>BSD-2-Clause</includedLicense>
                        <includedLicense>BSD-3-Clause</includedLicense>
                        <includedLicense>BSD License 3</includedLicense>
                        <includedLicense>The BSD License</includedLicense>
                        <!-- Eclipse Public License variants -->
                        <includedLicense>Eclipse Public License 1.0</includedLicense>
                        <includedLicense>Eclipse Public License 2.0</includedLicense>
                        <includedLicense>Eclipse Public License - v 1.0</includedLicense>
                        <includedLicense>Eclipse Public License v2.0</includedLicense>
                        <includedLicense>EPL-1.0</includedLicense>
                        <includedLicense>EPL-2.0</includedLicense>
                        <includedLicense>EPL 1.0</includedLicense>
                        <includedLicense>EPL 2.0</includedLicense>
                        <!-- Eclipse Distribution License -->
                        <includedLicense>Eclipse Distribution License - v 1.0</includedLicense>
                        <includedLicense>EDL 1.0</includedLicense>
                        <!-- Public Domain -->
                        <includedLicense>Public Domain</includedLicense>
                        <includedLicense>CC0-1.0</includedLicense>
                        <!-- CDDL (common in Java ecosystem) -->
                        <includedLicense>CDDL-1.0</includedLicense>
                        <includedLicense>CDDL-1.1</includedLicense>
                        <includedLicense>Common Development and Distribution License 1.0</includedLicense>
                        <includedLicense>Common Development and Distribution License 1.1</includedLicense>
                        <includedLicense>CDDL+GPL License</includedLicense>
                        <includedLicense>CDDL + GPLv2 with classpath exception</includedLicense>
                        <!-- GPL with Classpath Exception (safe for Java) -->
                        <includedLicense>GPL2 w/ CPE</includedLicense>
                        <includedLicense>GPLv2+CE</includedLicense>
                        <includedLicense>GNU General Public License, version 2 with the GNU Classpath Exception</includedLicense>
                        <!-- LGPL (weak copyleft - generally safe for use) -->
                        <includedLicense>LGPL-2.1</includedLicense>
                        <includedLicense>LGPL-3.0</includedLicense>
                        <includedLicense>GNU Lesser General Public License</includedLicense>
                        <includedLicense>GNU Library General Public License</includedLicense>
                    </includedLicenses>
                    <failOnBlacklist>true</failOnBlacklist>
                    <failOnMissing>false</failOnMissing>
                </configuration>
            </plugin>
            <!-- PIT Mutation Testing (run with: mvn pitest:mutationCoverage) -->
            <plugin>
                <groupId>org.pitest</groupId>
                <artifactId>pitest-maven</artifactId>
                <version>${pitest.version}</version>
                <dependencies>
                    <dependency>
                        <groupId>org.pitest</groupId>
                        <artifactId>pitest-junit5-plugin</artifactId>
                        <version>${pitest-junit5.version}</version>
                    </dependency>
                </dependencies>
                <configuration>
                    <targetClasses>
                        <param>com.example.hello.*</param>
                    </targetClasses>
                    <targetTests>
                        <param>com.example.hello.*</param>
                    </targetTests>
                    <excludedClasses>
                        <param>com.example.hello.HelloWorldApplication</param>
                        <param>com.example.hello.adapter.ApplicationConfiguration</param>
                    </excludedClasses>
                    <excludedTestClasses>
                        <param>com.example.hello.architecture.*</param>
                    </excludedTestClasses>
                    <mutationThreshold>90</mutationThreshold>
                    <coverageThreshold>90</coverageThreshold>
                    <outputFormats>
                        <outputFormat>HTML</outputFormat>
                        <outputFormat>XML</outputFormat>
                    </outputFormats>
                    <timestampedReports>false</timestampedReports>
                    <!-- Override argLine to avoid JaCoCo variable interpolation issues -->
                    <argLine>--add-opens java.base/java.lang=ALL-UNNAMED</argLine>
                </configuration>
            </plugin>
        </plugins>
    </build>

    <profiles>
        <profile>
            <id>run</id>
            <build>
                <defaultGoal>spring-boot:run</defaultGoal>
            </build>
        </profile>
    </profiles>
</project>
